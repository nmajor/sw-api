<% @chronicles.each_with_index do |chronicle, index| %>
<a class="btn btn-default btn-lg" href="/chronicle/<%= "#{chronicle.id}"%>"><%= "#{chronicle.first_name}" %> <%= "#{chronicle.last_name}" %></a></div>
<%end%>